> **STATUS (2026-09-09):** Superseded by hermes-token-optimization-plan.md (which corrects its first cut).

# Hermes Token Optimization — New-Session Handoff

## Mission

Continue from the completed clean-slate Hermes reset. The next job is **audit only**: find the common places where Hermes is overconfigured, loading unnecessary context, or spending tokens on tools/skills/config that do not earn their keep. Do not change configuration until the audit produces a ranked list and Stephen approves the cuts.

## Verified current state

- Hermes home: `/Users/stephenbowman/.hermes`
- `HERMES_HOME` is set to that path.
- Hermes config is clean-slate/default: config version 37, no model pin, OpenRouter shown as the default provider.
- Gateway is running under launchd.
- 24 profiles exist: default plus analyst, archivist, backend, bookie, builder, cfblocalworker, critic, curator, design, frontend, ops, pliny, pocock, reader, researcher, reviewer, scoreboard, scout, scribe, studio, swarm-forge, sysop, and workbench.
- All 24 profiles reset and synced successfully; each regenerated default config/SOUL and bundled skills.
- Live checks after reset:
  - MCP servers: none configured.
  - Cron jobs: none scheduled.
  - Webhook platform: disabled.
  - User plugins/hooks: absent from live config directories.
  - Skills: 0 hub-installed, 0 local; bundled skills only.
- The clean runtime still has 32 session records. Do not delete or prune them during the token audit.

## Backup / restore

Full backup: `/Users/stephenbowman/agent-config-backup-2026-08-20`

Key files:

- `RESTORE.md` — combined restore document with the complete Hermes table.
- `RESTORE-HERMES.md` — focused Hermes restore document.
- `_HERMES_COMPLETE_MANIFEST.json` — 627-item authoritative manifest.
- `restore-hermes.py` — verified restore script.

Restore, only if explicitly requested:

```bash
BACKUP="/Users/stephenbowman/agent-config-backup-2026-08-20"
/Users/stephenbowman/.local/bin/hermes gateway stop
python3 "$BACKUP/restore-hermes.py"
```

## Never touch during the audit

- `~/.hermes/.env`, `.env` backups, `auth.json`, auth backups, `auth/`, `shared/`, Google OAuth files, `mcp-tokens/`, `secrets/`
- Profile `.env` and `auth.json`
- `sessions/`, logs, `state.db`, `lcm.db`, `memory_store.db`, `verification_evidence.db`, caches, pairing data, generated profile aliases
- `/Users/stephenbowman/repos/cfb-model-lab` and its Git state
- The backup directory itself

The 24 memory directories were restored to their original locations. All 74 memory files were verified byte-for-byte against the backup copy. Preserve them.

## Audit questions for the new session

### 1. Prompt assembly cost

Measure actual prompt contribution, not just file sizes:

- Base system prompt size with default config.
- Contribution from SOUL, memory, user profile, project `AGENTS.md`, skills metadata, tool instructions, runtime footer, and platform-specific overlays.
- Difference between a normal default session and a named profile session.
- Whether all 81 bundled skills are injected, merely indexed, or loaded only on demand.
- Whether `.skills_prompt_snapshot.json` is runtime cache only or contributes to live prompt assembly.

### 2. Tool and toolset overhead

Inventory enabled toolsets in the live default and each profile, then estimate schema-token cost:

- Which toolsets are present by default?
- Which are duplicated across profiles?
- Which high-cost surfaces are unused: browser, vision, image generation, video, TTS, computer use, delegation, MCP, or large messaging surfaces?
- Is there a profile-level tool policy outside `config.yaml` that still affects the prompt?
- Compare tool-schema size with actual usage from non-sensitive logs/insights.

### 3. Configuration discovery overhead

Check whether the following cause startup work or prompt bloat even when unused:

- Provider/model catalogs and fallback provider lists.
- Large API-key pools or credential-provider discovery.
- Platform sections left enabled by environment variables.
- Desktop/Bot Mode roster metadata and profile discovery.
- Project scanning and repo-context discovery.
- Dynamic memory provider configuration.
- Context-engine selection and LCM/plugin discovery.
- Empty/default directories regenerated by Hermes.

Do not print keys, tokens, or `.env` values. Report counts and names only where safe.

### 4. Profile duplication

Determine whether the 23 named profiles each carry their own copy of bundled skill trees and default config, and whether that affects tokens or only disk/startup cost. Separate:

- Actual per-turn prompt cost.
- Startup/discovery cost.
- Disk duplication.
- Operational complexity.

Do not remove profiles. Stephen needs the roster and signed-in state intact.

### 5. External context files

Re-check context-file precedence from the live source/docs and identify every file that can be injected from:

- Home directory.
- Hermes home.
- Active project root and parents.
- `.cursor`, `.claude`, `.codex`, and `.agents` compatibility paths.

The project repo currently has only `AGENTS.md`; do not edit or move it.

## Required output from the next session

Produce a ranked table:

| Rank | Source of waste | Verified size/cost | Active on which profiles | Recommendation | Risk |
|---|---|---:|---|---|---|

Rules:

1. Measure before recommending cuts.
2. Separate token waste from disk waste and latency waste.
3. Do not conflate an installed skill catalog with injected skill content.
4. Do not disable MCP, tools, profiles, memory, or providers automatically.
5. Surface the smallest reversible cut first.
6. End with one concrete next action for Stephen.

## Known constraints / preferences

Stephen wants direct, ranked findings in plain language. Native Hermes features beat custom infrastructure. OpenRouter is emergency-only. Preserve intentional work and backups. No credentials in output. No parlay-style configuration sprawl: one clean measured cut beats ten speculative tweaks.
